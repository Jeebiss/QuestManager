package net.jeebiss.questmanager.quests;

public class GoalBuilder {

	public GoalBuilder() {
		// will parse list of goals, and build the proper listeners
		//DenizenAPI.getCurrentInstance().getCommandRegistry().getCommand(ListenCommand.class).parseArgs(scriptentry);
	}

}
